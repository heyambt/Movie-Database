import React from 'react';


const MovieGrid = (props) => {
    return (
        <div>
       
        </div>
    )
}
export default MovieGrid;

/*
const WeatherGrid = ({ weather }) => {

    const weatherDivs = (arr) => {
        return arr.map((day, i) => {
            return (
                <div className={`day day-0${i + 1}`} key={i}>
                    <figure><img src={`${iconPath}${day.icon}`} alt={day.weather} /></figure>
                    <div className="date">{day.date}</div>
            <div className="temps"><span className="temp">{day.high}°</span></div>
                </div>
            );
        });
    }

    return (
        <div className="five-day-weather-grid">
            {weatherDivs(weather)}
        </div>
    );

};
*/
